import * as express from "express";
const router = express.Router();

router.get("/", (req, res) => {
  // get publications
});

router.get("/:id", (req, res) => {
  // get publication
});

router.post("/", (req, res) => {
  // create publication
});

export default router;
